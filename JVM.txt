public class JvmComprehension { // -1 

    public static void main(String[] args) { // 0 
        int i = 1;                      // 1 
        Object o = new Object();        // 2
        Integer ii = 2;                 // 3
        printAll(o, i, ii);             // 4
        System.out.println("finished"); // 7
    }

    private static void printAll(Object o, int i, Integer ii) {
        Integer uselessVar = 700;                   // 5
        System.out.println(o.toString() + i + ii);  // 6
    }
}

/*
-1 - Происходит загрузка класса JvmComprehension в Metaspace. Подсистема загрузчиков классов подгружает класс, затем подготавливает класс к выполнению и инициализирует static элементы класса
0 - Создаётся фрейм main() в стеке.
1 - В стеке во фрейме main() создаётся переменная int i со значением 1.
2 - Происходит загрузка класса Object в Metaspace. Подсистема загрузчиков классов подгружает класс, затем подготавливает класс к выполнению и инициализирует static элементы класса
    В куче создаётся объект типа Object. В стеке создаётся переменная o и ей присваевается ссылка на Object в куче.
3 - Происходит загрузка класса Integer в Metaspace. Подсистема загрузчиков классов подгружает класс, затем подготавливает класс к выполнению и инициализирует static элементы класса
    В куче создаётся объект типа Integer. В стеке создаётся переменная ii и ей присваевается ссылка на Integer в куче.
4 - Создаётся фрейм printAll() в стеке.
    В стеке во фрейме printAll() создаются переменные o (ссылка на существующий Object в куче), i (переменная со значением 1), ii (ссылка на существующий Integer в куче)
5 - В куче создаётся объект типа Object. В стеке создаётся переменная uselessVar и ей присваевается ссылка на Object в куче.
6 - Создаётся фрейм println() в стеке. Создаётся в куче объект типа String.
    Создаётся фрейм toString() в стеке. Результат создаётся в куче
    Фрейм toString() удаляется. Ссылка на результат toString() передается во фрейм println()
    Фрейм println() удаляется. GC удаляет объект String из кучи.
    Фрейм printAll удаляется.
    GC удаляет из кучи объекты Integer (на коротый ссылалась uselessVar) Integer (на коротый ссылалась ii) Object (на коротый ссылалась о)
7 - Создаётся фрейм println() в стеке. Создаётся в куче объект типа String со значением "finished" и ссылка передается во фрейм println().
    Фрейм println() удаляется. 
    GC удаляет из кучи объект типа String со значением "finished"
    Фрейм main удаляется.
*/
